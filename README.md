# CURSO DE POO COM JAVA 📖

JAVA
```
Java é uma linguagem de programação e plataforma de computação.
```
IDES
```
IDE ou Integrated Development Environment (Ambiente de Desenvolvimento Integrado) é um software que auxilia no desenvolvimento de aplicações,
muito utilizado por desenvolvedores, com o objetivo de facilitar diversos processos (ligados ao desenvolvimento).
```

OBJETOS NO JAVA
```
Um objeto é a representação (instância) de uma classe, Uma instância é um objeto do tipo definido pela classe. Qualquer classe pode ser instanciada como qualquer outro tipo de dado da linguagem Java.
```
CLASSE
```
Classe é um modelo ou especificação que define um tipo de objeto.Uma classe é um elemento do código Java que utilizamos para representar objetos do mundo real. Dentro dela é comum declararmos atributos e métodos, que representam, respectivamente,
as características e comportamentos desse objeto.
```
TIPOS DE DADOS
```
são divididos em por valor (tipos primitivos) e por referência (tipos por referência)
Os tipos primitivos são boolean, byte, char, short, int, long, float e double. Os tipos por referência, são classes que especificam os tipos de objeto Strings, Arrays Primitivos e Objetos.
```
MÉTODO
```
Um método em Java é equivalente a uma função, subrotina ou procedimento,  um conjunto de códigos agrupados para executar uma determinada tarefa ou operação.
```
HERANÇA
```
Herança é a capacidade de uma subclasse de ter acesso as propriedades da superclasse.
```
CLASSE ABSTRATA(abstract)
```
É um tipo de classe especial que não pode ser instanciada, apenas herdada. 
```
OVERRIDE
```
A sobrescrita (ou override) está diretamente relacionada à orientação a objetos, 
mais especificamente com a herança. Com a sobrescrita, conseguimos especializar os métodos herdados das superclasses, alterando o seu comportamento nas subclasses por um mais específico.
```
ENCAPSULAMENTO
```
O encapsulamento é um conceito da Programação Orientada a Objetos onde o estado de objetos (as variáveis da classe) e seus comportamentos (os métodos da classe) são agrupados em conjuntos segundo o seu grau de relação.
```
COLLECTIONS
```
Em Java , a coleção também é um objeto. Os três grandes tipos de coleções são: Lista ( também chamado de “sequência“); • Conjunto; • Mapa( também chamado de “dicionário“).
```
INTERFACE
```
A interface é um recurso muito utilizado em Java, bem como na maioria das linguagens orientadas a objeto, para “obrigar” a um determinado grupo de classes a ter métodos ou propriedades em comum para existir em um determinado contexto, contudo os métodos podem ser implementados em cada classe de uma maneira diferente.
```
ENUM
```
São tipos de campos que consistem em um conjunto fixo de constantes (static final), sendo como uma lista de valores pré-definidos. Na linguagem de programação Java, pode ser definido um tipo de enumeração usando a palavra chave enum. Todos os tipos enums implicitamente estendem a classe java.
```
STREAMS
```
é uma API introduzida no Java 8 (fonte) que fornece uma maneira mais "funcional" e "expressiva" de lidar com coleções de objetos em Java. O que quero dizer com isso é que o Java Stream fornece uma maneira mais moderna, elegante e concisa de lidar com operações em coleções de objetos em Java.
```
SOLID
```
SOLID é um acrônimo para cada um dos cinco princípios que fazem parte desse grupo: Single Responsability Principle (Princípio da Responsabilidade Única); Open/Closed Principle (Princípio do “Aberto para Extensão/Fechado para Implementação); Liskov Substitution Principle (Princípio da Substituição de Liskov).
```